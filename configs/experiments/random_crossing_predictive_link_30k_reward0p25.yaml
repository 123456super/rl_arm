# P2.2 predictive training-signal probe. Validation-only candidate.
includes:
  - random_crossing_predictive_link_30k_tuned.yaml

sac:
  predictive_risk_penalty: 0.25

train:
  output_dir: outputs/current_results/p2_predictive_reward_shaping/train
  run_name: predictive_h0p5_reward0p25_seed101_steps30000
