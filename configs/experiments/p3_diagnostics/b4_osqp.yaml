# B4 diagnostic rerun using the optional OSQP QP backend.
includes:
  - ../p3_100k/b4_predictive_nonrobust_filter.yaml

env:
  safety_filter:
    use_qp_solver: true
    fallback_projection_iterations: 640
