# Offline-only strict diagnostic at a fixed 0.05 m/s obstacle speed.
# It keeps the final geometry and every robot-side constraint unchanged.
includes:
  - b4_root_cause_corrected_geometry_moving.yaml

env:
  obstacle:
    speed_range: [0.05, 0.05]

eval:
  method: link_fixed
  episodes: 20
