# Targeted diagnostic only. Expand the bounded goal-region search to the full
# unchanged success ball; do not alter d_safe, success tolerance, collision
# rules, horizon, state machine, or safety-filter semantics.
includes:
  - s1_static_revised_ik_goal_region_t005_clearance_retain_terminal_hard_cases_v1.yaml

architecture_version: hierarchical_s1_static_revised_ik_goal_region_t005_expanded_diagnostic_v1

env:
  hierarchical_control:
    architecture_version: hierarchical_s1_static_revised_ik_goal_region_t005_expanded_diagnostic_v1
    parent_architecture: hierarchical_s1_static_revised_ik_goal_region_t005_clearance_retain_terminal_hard_cases_v1
    planner:
      ik_goal_region_attempts: 384
      ik_goal_region_radius_m: 0.055
      candidate_selection: clearance_then_length
      goal_region_candidate_selection: clearance_then_length

train:
  output_dir: outputs/hierarchical/s1_static_revised_ik_goal_region_t005_expanded_diagnostic_v1

eval:
  seed_manifest: configs/experiments/reaching_recovery/manifests/v1_hierarchical_goal_region_expanded_cases.json
  nominal_only: true
