# Targeted smoke: use the high-clearance retained terminal and a low-gain
# waypoint command so the strict predictive filter can continue approaching it.
includes:
  - s1_static_revised_ik_goal_region_t005_clearance_terminal_9006_v1.yaml

architecture_version: hierarchical_s1_static_revised_ik_goal_region_t005_clearance_slow_terminal_9006_v1

env:
  hierarchical_control:
    architecture_version: hierarchical_s1_static_revised_ik_goal_region_t005_clearance_slow_terminal_9006_v1
    parent_architecture: hierarchical_s1_static_revised_ik_goal_region_t005_clearance_terminal_9006_v1
    tracker:
      goal_region_hold_waypoint_gain: 0.25
      servo_trigger_m: 0.055

train:
  output_dir: outputs/hierarchical/s1_static_revised_ik_goal_region_t005_clearance_slow_terminal_9006_v1

eval:
  seed_manifest: configs/experiments/reaching_recovery/manifests/v1_hierarchical_obstacle_aware_9006.json
  nominal_only: true
