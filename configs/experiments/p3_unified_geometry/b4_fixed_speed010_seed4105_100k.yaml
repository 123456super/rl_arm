# Independent training replicate for the canonical fixed-speed condition.
includes:
  - b4_fixed_speed010_base.yaml

seed: 4105

train:
  total_steps: 100000
  save_interval: 5000
  output_dir: outputs/p3_unified_geometry/b4_fixed_speed010_seed4105_100k

checkpoint_selection:
  seed: 5201
  episodes: 20
  metric: mean_reward

eval:
  method: link_fixed
  episodes: 20
  seed: 5101