includes:
  - s1_static_terminal_repaired_finetune_base.yaml

seed: 4303
device: cuda:0

train:
  total_steps: 820000
  output_dir: outputs/reaching_incremental/s1_static_terminal_repaired_finetune/train/seed_4303
  run_name: link_fixed_static_terminal_repaired_finetune_seed4303_steps820000
