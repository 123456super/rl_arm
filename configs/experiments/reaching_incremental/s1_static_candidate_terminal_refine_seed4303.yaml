includes:
  - s1_static_candidate_terminal_refine_base.yaml

seed: 4303
device: cuda:auto
device_selection:
  min_free_memory_gb: 6.0

train:
  total_steps: 540000
  output_dir: outputs/reaching_incremental/s1_static_candidate_terminal_refine/train/seed_4303
  run_name: link_fixed_static_candidate_terminal_refine_seed4303_steps540000
