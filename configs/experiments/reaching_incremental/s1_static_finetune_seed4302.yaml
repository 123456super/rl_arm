includes:
  - s1_static_obstacle_finetune_base.yaml

seed: 4302
device: cuda:auto
device_selection:
  min_free_memory_gb: 6.0

train:
  total_steps: 480000
  output_dir: outputs/reaching_incremental/s1_static_obstacle_finetune/train/seed_4302
  run_name: link_fixed_static_obstacle_finetune_seed4302_steps480000
