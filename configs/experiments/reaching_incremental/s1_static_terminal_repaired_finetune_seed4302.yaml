includes:
  - s1_static_terminal_repaired_finetune_base.yaml

seed: 4302
device: cuda:4

train:
  total_steps: 680000
  output_dir: outputs/reaching_incremental/s1_static_terminal_repaired_finetune/train/seed_4302
  run_name: link_fixed_static_terminal_repaired_finetune_seed4302_steps680000
