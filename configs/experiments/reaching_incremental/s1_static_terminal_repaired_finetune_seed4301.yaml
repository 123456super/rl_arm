includes:
  - s1_static_terminal_repaired_finetune_base.yaml

seed: 4301
device: cuda:1

train:
  total_steps: 720000
  output_dir: outputs/reaching_incremental/s1_static_terminal_repaired_finetune/train/seed_4301
  run_name: link_fixed_static_terminal_repaired_finetune_seed4301_steps720000
