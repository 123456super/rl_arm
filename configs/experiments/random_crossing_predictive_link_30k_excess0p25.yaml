# P2.2 lead-only shaping probe: penalize predicted risk above current risk.
includes:
  - random_crossing_predictive_link_30k_tuned.yaml

sac:
  predictive_risk_penalty: 0.25
  predictive_risk_penalty_mode: excess

train:
  output_dir: outputs/current_results/p2_predictive_excess_shaping/train
  run_name: predictive_h0p5_excess0p25_seed101_steps30000
