# UR5 短训练配置。
# 用途：验证真实 UR5 模型、奖励、风险和日志链路是否正常，不作为正式实验结果。
includes:
  - ../ur5.yaml

train:
  method: ldrc_adaptive
  total_steps: 10000
  progress_interval: 100
  log_interval: 10
  save_interval: 5000
  output_dir: outputs
  run_name: null
