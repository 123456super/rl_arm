# P3 B2, 100k-step development replication. Keeps B2 factors unchanged.
includes:
  - ../p3/b2_link_current.yaml

train:
  total_steps: 100000
  output_dir: outputs/p3_dev_100k/b2_link_current

checkpoint_selection:
  seed: 5201
  episodes: 20
  metric: mean_reward
