# P3 B4, 100k-step development replication. Keeps B4 factors unchanged.
includes:
  - ../p3/b4_predictive_nonrobust_filter.yaml

train:
  total_steps: 100000
  output_dir: outputs/p3_dev_100k/b4_predictive_nonrobust_filter

checkpoint_selection:
  seed: 5201
  episodes: 20
  metric: mean_reward
