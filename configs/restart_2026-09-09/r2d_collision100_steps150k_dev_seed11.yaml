includes:
  - r2c_collision_terminal_penalty100_dev_seed11.yaml

# R2c removed the collision terminal shortcut and both validation success
# curves were still rising at 100k.  R2d changes only the from-scratch training
# budget; it does not resume without replay state and does not alter the MDP.
train:
  total_steps: 150000
  output_dir: outputs/restart_2026-09-09/r2d_training_budget_150k/train
  run_name: penalty1_dropout20_collision100_dev_seed11_steps150000
