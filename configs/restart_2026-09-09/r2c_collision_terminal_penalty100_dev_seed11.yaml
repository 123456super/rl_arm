includes:
  - r2b_penalty1_dropout20_dev_seed11.yaml

# R2b exposed a terminal-shortcut failure: after 50k steps, collision episodes
# ended after 51 steps on average with a higher undiscounted return than 240-step
# timeouts.  Change only the terminal collision task penalty.  The 20%
# no-obstacle coverage, current-risk cost, fixed-risk weight, seed, RTB and
# 100k-step budget remain unchanged.
reward:
  collision_penalty: 100.0
train:
  output_dir: outputs/restart_2026-09-09/r2c_collision_terminal_penalty/train
  run_name: penalty1_dropout20_collision100_dev_seed11_steps100000
