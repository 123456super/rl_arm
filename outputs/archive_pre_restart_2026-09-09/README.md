# 重启前实验输出归档

> 封存日期：2026-09-09  
> 状态：只读历史资产，不作为重启后论文结果、validation 选择或方法门控依据。

本目录保存重启前 `outputs/` 中的全部内容：

- `current_results/`：封存前曾作为当前结果使用的训练、评估和汇总输出；
- `archive_old/`：更早期的训练、失败实验、旧 checkpoint 和部署预检。

使用规则：

- 允许读取，用于代码审计、问题定位和历史复现。
- 不得把其中数值复制到新实验结果事实源。
- 不得在原目录内续跑或覆盖文件；如需复现，使用新的实验版本和独立输出目录。
- 旧配置中的路径已改为本归档位置，只用于解析历史输入/checkpoint；启动旧配置前仍须人工指定新的输出目录。
- 新实验统一写入 `outputs/restart_2026-09-09/`。

对应历史文档：

```text
docs/experiments/archive_pre_restart_2026-09-09/
```
